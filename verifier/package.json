{
  "name": "@ashlar/verifier",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Standalone tool that independently re-validates a workflow's proof chain directly against the ledger, with no login and no trust in Ashlar's own systems required.",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@anchor-lang/core": "^1.1.2",
    "@solana/web3.js": "^1.98.4",
    "js-sha3": "^0.13.0"
  }
}
