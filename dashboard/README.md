# @ashlar/dashboard

Live public dashboard that streams step attestations in real time as they land on-chain, using
Helius WebSockets. Gives an outside observer a live view of a workflow's proof chain without
needing to run `@ashlar/verifier` themselves.

Frontend framework choice is deferred until Phase 6.

**Status: not yet implemented (Phase 6).**
