/**
 * Phase 6 — Live Public Dashboard.
 *
 * Uses Helius WebSockets to stream step attestations in real time as they
 * land on-chain, giving anyone a live view of a workflow's proof chain
 * without needing to run the verifier themselves.
 *
 * Frontend framework choice is deferred until Phase 6.
 *
 * Status: not yet implemented (Phase 6).
 */
export function startDashboard(): never {
  throw new Error('startDashboard is not yet implemented (Phase 6)');
}
